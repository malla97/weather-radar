import Current from "./Current";
import Forecast from "./Forecast";

const Weather = () => {
    return (
        <div>
            <Current />
            <Forecast />
        </div>
    );
}

export default Weather;